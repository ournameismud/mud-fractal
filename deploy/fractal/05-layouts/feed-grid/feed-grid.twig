<div class="l-feed-grid" {% if dev %}devmode{% endif %}>
  {% for item in entries %}
    <div class="l-feed-grid__cell">
      {% include '@' ~ item.template with item.data %}
    </div>
  {% endfor %}
</div>